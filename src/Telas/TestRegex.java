/*
Este arquivo foi criado em 15/03/2018 por Taffarel Xavier.
 * 
 */
package Telas;

import java.util.regex.Matcher;
import java.util.regex.Pattern;
import tx.utilitarios.*;
/**
 *
 * @author Taffarel <taffarel_deus@hotmail.com>
 */
public class TestRegex {
/*
    public static void main(String[] args) {

        // String to be scanned to find the pattern.
        String line = "public static void main(String[] args){adfafads}";
        String pattern = "(.*)((public\\s+static\\s+void\\s+main\\(String\\[\\]\\s+\\w+\\)|\n))(.*)";

        // Create a Pattern object
        Pattern r = Pattern.compile(pattern);

        // Now create matcher object.
        Matcher m = r.matcher(line);
        if (m.find()) {
            System.out.println("Found value: " + m.group(0));
            System.out.println("Found value: " + m.group(1));
            System.out.println("Found value: " + m.group(2));
        } else {
            System.out.println("NO MATCH");
        }
  
        
    }
*/
}
